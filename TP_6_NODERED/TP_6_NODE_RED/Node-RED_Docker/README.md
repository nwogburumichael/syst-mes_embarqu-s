# Node-RED_Docker

My Node-RED config to use with Docker-Compose including:

- Mosquitto (MQTT)
- Node-RED
- Mariadb (Database)
- phpMyAdmin (Gui for db)

For tests purpose only!

Command:

> docker-compose up -d
